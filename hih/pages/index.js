/* ==========================================
ROOT PAGE
==========================================*/
function HomePage() {
  return (
  <div>
    HEART ♥ IS HOME
  </div>
  )
}
export default HomePage
/* ==========================================
STEP (1)
  I N T R O   C A R D > .
    FULLSCREEN HERO TEXT APPEARS ON SCREEN, 
      vanilla css text transform ?
      or 
      functional component > 
        timer w/ fade, then 'display: none' attribute?
    TEXT FADES TO BLACK
STEP (2)
    PUSH USER TO '/signup'
    PUSH USER TO '/login'
    PUSH USER TO '/home' > 
      our beloved logged-in user's personal dashboard
========================================== */
